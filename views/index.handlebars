
<div class="jumbotron">
    <h1 class="display-4">Bem vindo ao Node Blog!</h1>
    <p class="lead">Este é um blog desenvolvido em Node + Express + MongoDB</p>
    <hr class="my-4">
    <p>Aqui você poderá ter atualizações sobre o mundo da computação</p>
    <a class="btn btn-primary btn-lg" href="/user/register" role="button">Criar conta</a>
</div>
<hr>
<h2>Posagens recentes: </h2>
{{#each posts}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{title}}</h4>
            <p>Descrição: {{description}}</p>
            <a href="/post/{{slug}}"><button class="btn btn-success">Ler mais</button></a>
            <hr>
            <small>Categoria: {{category.name}}</small><br>
            <small>Data: {{date}}</small><br>
        </div>
    </div>

{{else}}
    <h4 class="mt-4">Nenhum post registrado :(</h4>
{{/each}}

